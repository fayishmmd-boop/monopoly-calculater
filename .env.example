# Example environment variables (do NOT commit secrets)
MONOPOLY_SECRET=change_me_to_a_random_secret
# Optional: set a DATABASE_URL for production (uncomment and edit)
# DATABASE_URL=sqlite:///monopoly.db
# DATABASE_URL=postgresql+psycopg2://user:pass@host:5432/dbname
# Optional: set the PORT when running in some PaaS environments
# PORT=8080
